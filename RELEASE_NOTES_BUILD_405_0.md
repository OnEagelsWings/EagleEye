# Release notes — Build 405.0

Adds Source Registry v2 metadata and closes the 401–405 feedback cycle. Existing governed registry sources are augmented with capability, authentication, rate-limit, usage-policy, provenance and health metadata. Health starts as unknown until observed; registration does not validate or enable live access. No new automatic network authority is introduced.

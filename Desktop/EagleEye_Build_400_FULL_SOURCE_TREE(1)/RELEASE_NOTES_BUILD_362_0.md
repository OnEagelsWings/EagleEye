# EagleEye Build 362.0 — PostgreSQL Team Profile

Build 362 establishes the Phase-16 PostgreSQL team migration and validation path while retaining SQLite as the portable default. It includes deterministic migration planning, a real psycopg live-validation harness, concurrent-connection probe, local backup/restore rollback drill, infrastructure-aware AI dossiers, backend-secret OPSEC monitoring and crawler backend-awareness.

The current build environment has no PostgreSQL server and no psycopg driver. External PostgreSQL live validation is therefore not claimed and remains `not_run`.

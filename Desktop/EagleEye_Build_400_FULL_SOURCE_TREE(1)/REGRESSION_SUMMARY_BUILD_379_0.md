# Regression Summary — Build 379.0

Build-378 test suite under the Build-379 runtime: **43/44 PASS**.

The only failure is the expected historical version assertion (`378.0 / 378.0.0` expected by the old test while the runtime correctly reports `379.0 / 379.0.0`). All **43 functional** Build-378 voice/crawler intent, image/media, dossier, workflow, entity, AI and OPSEC tests pass.

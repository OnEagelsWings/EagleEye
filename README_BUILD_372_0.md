# EagleEye Intelligence Platform – Build 372.0

Build 372 adds a conservative evaluation layer for Entity Resolution v2 and continues the mandatory crawler expansion through Build 380.

Main additions:
- synthetic holdout corpus and false-link metrics;
- common-name and strong-identifier-conflict cohorts;
- crawler source-quality calibration and per-lead quality packets;
- no automatic threshold tuning;
- no identity probability claims;
- no automatic merge or review bypass.

The holdout is synthetic and deterministic. It does not constitute external real-world validation.

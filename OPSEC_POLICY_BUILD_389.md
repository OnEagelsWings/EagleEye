# OPSEC Policy — Build 389

Result intake is local and network-silent. Quarantined artifacts are excluded from automatic parsing. Raw objects remain immutable and hash-bound. Promotion requires explicit `PROMOTE EVIDENCE` plus case-scoped source-review authority. Build 389 neither grants network authority nor weakens Build-386/387 GO/LIVE boundaries.

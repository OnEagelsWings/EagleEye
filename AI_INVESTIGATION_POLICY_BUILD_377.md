# AI Investigation Policy — Build 377

The AI investigator may consume case-scoped image validation summaries and provenance. It has no direct image-network authority, cannot automatically invoke reverse-image providers, cannot confirm a person's identity from image similarity, and cannot convert visual/geolocation hypotheses into scene-location facts. Human review remains mandatory.

# Regression Summary — Build 378.0

Build-377 test suite under the Build-378 runtime: **42/43 PASS**.

The only failure is the expected historical version assertion (`377.0 / 377.0.0` expected by the old test while the runtime correctly reports `378.0 / 378.0.0`). All 42 functional Build-377 image/media crawler, provenance, deduplication, pressure, AI and OPSEC tests pass.

# Phase 17 Progress – Build 386

Progress: **6/20**.

Build 386 adds capability-scoped GO grants over the Build-385 acquisition layer. Grants are case-scoped, source-scoped, budget-scoped, short-lived and bound to current Operations/OPSEC/workflow state. This creates the authorization boundary required before controlled autonomous execution is introduced in a later build.

# Regression Summary — Build 363.0

Build-362 regression: **18/20 PASS**.

The two failures are expected version-bound assertions requiring runtime/health to remain exactly `362.0`. PostgreSQL migration/rollback, AI, OPSEC, crawler and truthfulness functionality remain green.

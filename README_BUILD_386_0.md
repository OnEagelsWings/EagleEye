# EagleEye Build 386.0 – Capability-Scoped GO

Build 386 introduces a short-lived execution-grant layer between acquisition planning and external execution. It preserves all existing human review, workflow budget and OPSEC boundaries.

Key rule: `GO` authorizes only the exact capabilities encoded in the grant. Build 386 itself performs no external network work.
